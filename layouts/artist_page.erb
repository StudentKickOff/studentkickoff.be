<% render '/partials/header.*', image: "/assets/images/artists/#{item[:image]}" do %>
<%= item[:name] %>
<% end %>
<div class="section-yellow">
  <div class="container">
    <h2><%= "#{pretty_time(item[:start])} | #{item[:stage]}" %></h2>
    <%= yield %>
  </div>
</div>
